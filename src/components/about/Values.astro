---
const values = [
  [
    {
      ref: '01',
      name: 'Freiheit durch Effizienz',
      description:
        'Wir glauben daran, dass durch den Einsatz intelligenter KI-Lösungen mehr Freiheit möglich ist. Unsere Technologie übernimmt die Aufgaben, damit du dich auf das Wesentliche konzentrieren kannst.',
    },
    {
      ref: '02',
      name: 'Maßgeschneiderte Lösungen',
      description:
        'Jedes Unternehmen ist einzigartig. Daher passen wir unsere KI-Lösungen individuell an deine Bedürfnisse an, um maximale Produktivität und Effizienz zu erreichen.',
    },
  ],
  [
    {
      ref: '03',
      name: 'Kontinuierliche Verbesserung',
      description:
        'Unsere Arbeit endet nicht mit der Einführung der Lösung. Wir bieten fortlaufenden Support und verbessern unsere Systeme ständig, um dir langfristig den besten Nutzen zu bieten.',
    },
    {
      ref: '04',
      name: 'Verantwortung und Vertrauen',
      description:
        'Wir übernehmen Verantwortung für die Qualität unserer Arbeit und bauen Vertrauen auf, indem wir transparente, sichere und zuverlässige Lösungen liefern.',
    },
  ],
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col gap-8 lg:gap-12">
      <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Unsere Werte</h2>
      <div class="grid gap-8 lg:grid-cols-2">
        {
          values.map((column, index) => (
            <div class:list={['space-y-8', index === 1 && 'lg:mt-16']}>
              {column.map((value) => (
                <div class="bg-primary-500/10 dark:bg-primary-400/10 flex flex-col gap-12 rounded-3xl px-6 py-8">
                  <p class="text-sm">{value.ref}</p>
                  <div class="flex flex-col gap-4">
                    <h3 class="text-xl font-medium">{value.name}</h3>
                    <p class="text-primary-950/70 dark:text-primary-200/70 text-base sm:text-lg">
                      {value.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>